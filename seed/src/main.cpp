#include <iostream>

int main(int argc, char* argv[]) {
    if (argc != 2) {
        std::cerr << "Usage: tdd_fizzbuzz_cpp path_to_input_file" << std::endl;
        return 1;
    }

    // TODO(you): 请将包含里程数据的文本文件作为程序的输入
    //            读取文件中的每一行作为一次输入
    //            打印到标准输出，并分行
    return 0;
}
